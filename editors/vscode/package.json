{
  "name": "termlab-lua",
  "displayName": "TermLab Plugin Support",
  "description": "Lua language support for TermLab terminal emulator plugins — completions, hover docs, and diagnostics.",
  "version": "0.1.0",
  "publisher": "an0nn30",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/an0nn30/conch"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "activationEvents": [
    "workspaceContains:**/*.lua"
  ],
  "main": "./out/extension.js",
  "extensionDependencies": [
    "sumneko.lua"
  ],
  "contributes": {
    "configuration": {
      "title": "TermLab Plugin Support",
      "properties": {
        "termlab.checkOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Run `termlab check` on save to validate plugin files."
        },
        "termlab.executablePath": {
          "type": "string",
          "default": "termlab",
          "description": "Path to the `termlab` executable (for `termlab check` diagnostics)."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
